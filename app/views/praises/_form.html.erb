<%= form_for(@praise) do |f| %>
  <% if @praise.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@praise.errors.count, "error") %> prohibited this praise from being saved:</h2>

      <ul>
      <% @praise.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
